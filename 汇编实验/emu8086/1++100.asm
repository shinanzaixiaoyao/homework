SSEG SEGMENT STACK
    DB 32 DUP(0)
SSEG ENDS

DSEG SEGMENT
    SUM DW 0
DSEG ENDS

CSEG SEGMENT
ASSUME CS:CSEG,DS:DSEG,SS:SSEG
START:
    MOV AX,DSEG
    MOV DS,AX
    XOR AX,AX
    
    MOV AX,SUM
    MOV BX,1
    MOV CX,100
    
LOOP_ADD:
    ADD AX,BX
    INC BX
    LOOP LOOP_ADD
    
    MOV SUM,AX
    
    MOV AH,4CH
    INT 21H
CSEG ENDS
END START